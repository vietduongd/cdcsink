# ============================================================
# CDC Application Configuration
# ============================================================

# Config Storage Backend
# Options: files, postgres, postgresql, db
CONFIG_STORAGE=files

# Database URL (only used if CONFIG_STORAGE=postgres)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/cdc

# ============================================================
# Logging
# ============================================================

# Log Level
# Options: trace, debug, info, warn, error
RUST_LOG=info

# ============================================================
# API Server
# ============================================================

API_HOST=0.0.0.0
API_PORT=3000
CORS_ENABLED=true

# ============================================================
# Development Settings
# ============================================================

# Configuration directory (for file-based storage)
CONFIG_DIR=config

# ============================================================
# Docker Settings
# ============================================================

# PostgreSQL (when using docker-compose)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=cdc

# NATS
NATS_URL=nats://localhost:4222

# PgAdmin
PGADMIN_EMAIL=admin@cdc.local
PGADMIN_PASSWORD=admin

# ============================================================
# SMTP Email Configuration (Optional - for error notifications)
# ============================================================
# If not set, email notifications will be disabled and errors will only be logged

# SMTP server hostname (e.g., smtp.gmail.com, smtp.office365.com)
SMTP_HOST=smtp.gmail.com

# SMTP server port (usually 587 for TLS, 465 for SSL)
SMTP_PORT=587

# SMTP authentication username (usually your email address)
SMTP_USERNAME=your-email@gmail.com

# SMTP authentication password or app password
SMTP_PASSWORD=your-app-password

# Sender email address (From field)
SMTP_FROM_EMAIL=cdc-alerts@your-domain.com

# Comma-separated list of recipient email addresses
SMTP_TO_EMAILS=admin@your-domain.com,ops@your-domain.com

# Example for Gmail:
# 1. Enable 2-factor authentication
# 2. Generate an app password: https://myaccount.google.com/apppasswords
# 3. Use the app password as SMTP_PASSWORD

# Example for Office 365:
# SMTP_HOST=smtp.office365.com
# SMTP_PORT=587
# SMTP_USERNAME=your-email@company.com
# SMTP_PASSWORD=your-password

